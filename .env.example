# Database
# PostgreSQL connection string.
# Supabase (Vercel/prod): usa il TRANSACTION pooler (non Session) per evitare "max clients reached".
# Dashboard Supabase → Settings → Database → Connection string → "Transaction" (porta 6543).
# Esempio: postgresql://postgres.[PROJECT]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
# Opzionali in URL: pgbouncer=true (consigliato), connection_limit=1 (già gestito da lib/prisma in prod).
DATABASE_URL=
# Vercel Blob Storage (opzionale, solo se usi upload immagini)
# Ottieni il token da: https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=
# Node Environment (impostato automaticamente da Vercel in produzione)
# NODE_ENV=production
